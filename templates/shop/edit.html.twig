{% extends 'base.html.twig' %}

{% block body %}
    <h1>Edit vacancy</h1>

    {{ include('shop/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-primary" href="{{ path('store_delete', { id: shop.id }) }}">Delete</a>
    <a class="btn btn-primary" href="{{ path('stores') }}">back to list</a>
{% endblock %}
