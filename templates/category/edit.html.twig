{% extends 'base.html.twig' %}

{% block body %}
    <h1>Edit category</h1>

    {{ include('category/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-primary" href="{{ path('category_delete', { id: category.id }) }}">Delete</a>
    <a class="btn btn-primary" href="{{ path('category') }}">back to list</a>
{% endblock %}
