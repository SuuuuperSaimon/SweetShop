{% block form%}
    {{ form_start(form) }}
    <div style="margin-bottom: 1rem;">
        <div>
            <div>
                {{ form_errors(form.district_name) }}
            </div>
            <div>
                {{ form_label(form.district_name) }}
            </div>
            <div>
                {{ form_widget(form.district_name) }}
            </div>
            <small>{{ form_help(form.district_name) }}</small>
        </div>
        <div style="margin: 2rem 0;">
            <button class="btn btn-primary">{{ button_label|default('Save') }}</button>
        </div>
        {{ form_end(form) }}
    </div>

{% endblock form %}

